# Language setting
language: node_js

# Version number
node_js:
  - 0.10.33

after_success:
  - if [ "$BRANCH" == "master" ];then git remote add dokku test@162.243.187.213:test; fi
  - if [ "$BRANCH" == "master" ]; then git push dokku master; fi
